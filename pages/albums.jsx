import Head from "next/head";

const Albums =() => {
    return (
      <div>
        <Head>
          <title>Albums | Music App</title>
        </Head>
        <main>Albums</main>
      </div>
    );
}

export default Albums;